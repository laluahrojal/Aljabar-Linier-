SPL 1 – NumPy (2 variabel)
# import numpy as np  
Fungsi: Mengimpor library NumPy.
Kegunaan: Untuk menghitung solusi SPL dengan matriks.

# A = np.array([[2, 3], [1, -1]])  
Fungsi: Membuat matriks koefisien.
Kegunaan: Menyimpan nilai dari x dan y di masing-masing persamaan.

# b = np.array([7, 1])  
Fungsi: Membuat vektor hasil.
Kegunaan: Menyimpan nilai sisi kanan dari kedua persamaan.

# solusi = np.linalg.solve(A, b)  
Fungsi: Menyelesaikan sistem persamaan linear.
Kegunaan: Mencari nilai x dan y secara numerik.

# print("Solusi (NumPy): x =", solusi[0], ", y =", solusi[1])  
Fungsi: Menampilkan hasil solusi.
Kegunaan: Memberi tahu nilai dari x dan y.

SPL 2 – SymPy (2 variabel)
# from sympy import symbols, Eq, solve  
Fungsi: Mengimpor fungsi dari SymPy.
Kegunaan: Untuk membuat variabel simbolik dan menyelesaikan SPL secara simbolik.

# x, y = symbols('x y')  
Fungsi: Mendefinisikan x dan y sebagai simbol.
Kegunaan: Bisa digunakan dalam bentuk persamaan matematika.

# eq1 = Eq(2*x + 3*y, 7)  
Fungsi: Membuat persamaan pertama.
Kegunaan: Merepresentasikan bentuk aljabar SPL.

# eq2 = Eq(x - y, 1)  
Fungsi: Membuat persamaan kedua.
Kegunaan: Bagian dari sistem SPL yang akan diselesaikan.

# jawaban = solve((eq1, eq2), (x, y))  
Fungsi: Menyelesaikan sistem persamaan.
Kegunaan: Menghitung nilai x dan y secara simbolik.

# print("Solusi (SymPy):", jawaban)  
Fungsi: Menampilkan hasil solusi.
Kegunaan: Mengetahui nilai x dan y dalam bentuk aljabar.

SPL 3 – NumPy (3 variabel)

# import numpy as np  
Fungsi: Mengimpor NumPy.
Kegunaan: Untuk menggunakan fungsi matriks.

# A = np.array([[1, 2, 1], [3, -1, 2], [-2, 3, -1]])  
Fungsi: Membuat matriks 3×3.
Kegunaan: Menyimpan koefisien x, y, z.

# b = np.array([10, 5, -9])  
Fungsi: Membuat vektor konstanta.
Kegunaan: Menyimpan nilai ruas kanan dari SPL.

# solusi = np.linalg.solve(A, b)  
Fungsi: Menyelesaikan sistem persamaan.
Kegunaan: Mencari nilai x, y, z.

# print("Solusi (NumPy): x =", solusi[0], ", y =", solusi[1], ", z =", solusi[2])  
Fungsi: Menampilkan hasil.
Kegunaan: Melihat solusi akhir.

SPL 4 – SymPy (3 variabel)
# from sympy import symbols, Eq, solve  
Fungsi: Mengimpor tools dari SymPy.
Kegunaan: Untuk menyusun dan menyelesaikan SPL simbolik.

# x, y, z = symbols('x y z')  
Fungsi: Mendefinisikan simbol x, y, z.
Kegunaan: Bisa digunakan untuk membuat persamaan.

# eq1 = Eq(x + 2*y + z, 10)  
Fungsi: Persamaan pertama.
Kegunaan: Salah satu bagian dari sistem.

# eq2 = Eq(3*x - y + 2*z, 5)  
Fungsi: Persamaan kedua.
Kegunaan: Melengkapi sistem 3 variabel.

# eq3 = Eq(-2*x + 3*y - z, -9)  
Fungsi: Persamaan ketiga.
Kegunaan: Menyusun sistem lengkap 3x3.

# solusi = solve((eq1, eq2, eq3), (x, y, z))  
Fungsi: Menyelesaikan 3 persamaan.
Kegunaan: Menemukan nilai x, y, z.

# print("solusi:", solusi)  
Fungsi: Cetak hasil.
Kegunaan: Menampilkan solusi dalam bentuk simbolik.
